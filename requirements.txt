# Laboratorio 4 - Analisis de Datos Geoespaciales (CC3084)
# Requiere Python 3.10-3.12 de 64 bits.
# En Python de 32 bits rasterio no tiene wheels y pip intenta compilar GDAL desde fuente (falla).

openeo>=0.51
truststore>=0.9      # usa el almacen de certificados del SO (evita fallos TLS con antivirus/proxy)
rasterio>=1.4
geopandas>=1.0
shapely>=2.0
folium>=0.19
mapclassify>=2.8
numpy>=2.0
pandas>=2.2
matplotlib>=3.9
seaborn>=0.13
scipy>=1.14

# Parte 2 (modelos de Machine Learning)
scikit-learn>=1.5
xgboost>=2.1
shap>=0.46
